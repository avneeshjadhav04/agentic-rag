# Backend base URL.
# In the single-container deployment, requests from the browser go to the same
# origin, so /api is proxied to the FastAPI backend by Next.js rewrites.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
